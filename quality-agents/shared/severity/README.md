# severity/ (reserved)

Future home for a shared severity vocabulary (P0-P3, health-score formula)
so every agent's findings are comparable at a glance, following the model
Wiring Guardian already uses internally. No implementation yet.
