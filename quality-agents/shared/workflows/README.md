# workflows/ (reserved)

Future home for a shared workflow-definition format, if a second agent needs
to walk the same end-to-end business workflows Wiring Guardian's
`workflows.config.json` already models. No implementation yet.
