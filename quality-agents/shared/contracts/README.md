# contracts/ (reserved)

Future home for a shared evidence/finding schema (e.g. the `layer|category|
file|route|observed` finding shape Wiring Guardian already uses) so multiple
agents can emit findings in one comparable format without importing each
other's engines. No implementation yet - reserved for when a second agent
needs it.
