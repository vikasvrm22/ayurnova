# evidence/ (reserved)

Future home for evidence-collection primitives genuinely reusable across
agents (e.g. generic file-tree walking or source parsing helpers), once a
second agent needs the same primitive an existing agent already implements.
No implementation yet.
