# reporting/ (reserved)

Future home for shared JSON/Markdown report rendering, if a second agent
needs the same report shape UI Judge and Wiring Guardian already produce
independently. No implementation yet.
