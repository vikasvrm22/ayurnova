# utils/ (reserved)

Future home for genuinely generic helpers (e.g. filesystem walking, path
resolution) only once a second agent needs the exact helper an existing
agent already has. Not a general dumping ground - see the top-level
[shared/README.md](../README.md) rule before adding anything here. No
implementation yet.
